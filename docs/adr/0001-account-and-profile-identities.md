# Keep account identity distinct from profile identity

The launcher represents existing Guild Wars logins as Accounts, each owning a default private Profile; adding an Account creates a local entry, not an ArenaNet registration. Profiles remain local configuration identities so alternative configurations for one login need not masquerade as additional Accounts. Additional configurations are not part of the agreed launcher MVP.
